# Документы проекта

- `spec.md` — спека 14.08.2026: постановка, решения, границы.
- `issues/01–06, 08` — тикеты сборки с комментариями исполнителей (методика, находки по движкам, решения).
- `PROGRESS.md` — таблица статусов тикетов.

Из исходного набора не включены тикет 07 (живой пилот) и `pilot-report.md`: они состоят из цифр
загрузки конкретных объектов. В остальных документах такие фрагменты вырезаны или обезличены,
имя заказчика заменено на роль; методика, архитектура и решения оставлены как есть.
Ссылки на `agent-runtime/…`, `docs/research/…`, `calendars-out/`, HANDOFF-документы — на файлы
рабочего репозитория, которых в витрине нет; форматы данных описаны в SCHEMA `occupancy/occupancy_core.py`.
