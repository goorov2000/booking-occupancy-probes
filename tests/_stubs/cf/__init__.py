"""Минимальная заглушка внешнего пакета `cf` — только для тестов витрины.

В рабочем репозитории `cf` — общий пакет контура (конфиг, транспорт телеграма,
журнал прогонов). Код модуля обращается к нему best-effort: `run_scheduled.send_telegram`
импортирует `cf.config.load_config` и `cf.notify.notify_telegram/mute_note`
внутри `try/except`, поэтому без пакета уведомление просто не уходит.

Несколько тестов подменяют `cf.notify.notify_telegram` через monkeypatch — для
этого нужен импортируемый модуль с тем же интерфейсом. Заглушка ведёт себя как
ненастроенный транспорт: ничего не отправляет и возвращает False. Подключается
в `tests/conftest.py` только если настоящий `cf` не импортируется.
"""
